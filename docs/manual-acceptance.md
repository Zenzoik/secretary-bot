# Ручная приёмка бота

Актуализировано: 06.09.2026. Бот: `@perssecretbot`. Тестовый контакт:
`@poldotk` (второй аккаунт), канал: `test secretary`.

Это основной файл сценариев и результатов ручной проверки. Источники:
[реестр готовности](rollout-readiness.md), [первый аудит](audit-2026-09-05.md),
[проверка аудита](audit-verification-2026-09-05.md). Старые результаты в
[stage-1](stage-1-live-checklist.md), [stage-1.7](stage-1.7-delivery.md) и
[stage-3](stage-3-delivery.md) — история, а не подтверждение текущей версии.

«Пройден» означает подтверждение владельцем/скриншотом или проверкой живого
журнала. Автотесты сами по себе не закрывают ручной сценарий. «Частично» означает,
что конкретный остаток указан ниже. Текущий rollout ещё не принят полностью.

## Подготовка и правила проверки

1. Использовать только выбранные тестовые чаты. Перед сценарием сверить `/status`
   и Mini App: режим, паузу, часовой пояс, персональное расписание и исключение.
2. По умолчанию `/dryrun`: превью получает владелец, контакт ничего не получает.
   Для автоответа в live отдельно включить «Увімкнути відповіді клієнтам» и
   подтвердить; после сценария вернуть `/dryrun`.
3. Ручной ответ и подтверждение платного обращения — реальные действия даже
   при dry-run. Тестовые начисления сохраняются в отчёте. Отказ владельца
   в текущей реализации не отменяет начисление.
4. Для сценариев во время задержки установить 60–60 секунд. После входящего
   выполнить действие через 10 секунд; итог проверять после 70 секунд.
5. Время устройства Europe/Prague и расписания Europe/Kyiv различается.
   В журнале ниже техническое время — UTC. Ожидаемые часы проверять в явно
   указанном часовом поясе.
6. Перед диагностикой разработчик проверяет `/readyz`, актуальный webhook,
   очередь обновлений и журнал конкретного тестового сообщения. При сбое
   сначала зафиксировать результат, затем повторять сценарий.

## Подтверждённые сценарии 06.09.2026

| ID | Действия для повтора | Ожидаемый и фактический результат | Подтверждение |
|---|---|---|---|
| M01 | `/start`, `/dryrun`, `/status`, открыть Mini App | Панель открывается; режим черновиков согласован в чате и панели | Скриншоты владельца |
| M02 | В активное окно отправить обычное сообщение со второго аккаунта | Одно превью владельцу; контакту ничего | `844372`, `dry_run` 08:42:54 UTC; подтверждено владельцем |
| M03 | Нажать «✅ Норм» на превью | Оценка показана на карточке, клавиатура убрана; ответ контакту не отправляется | Скриншот «Оброблено: оцінка Норм» |
| M04 | Поставить общую паузу и отправить сообщение | Нет превью/ответа | `skipped_kill_switch` 08:46:26 UTC |
| M05 | Возобновить бота и отправить сообщение в dry-run | Превью снова приходит | `844379`, `dry_run` 08:50:25 UTC; скриншот |
| M06 | Контакту поставить «Ніколи не відповідати», сохранить, отправить сообщение | Нет ответа и новой отложенной задачи | `844384`, `skipped_excluded` |
| M07 | Снять исключение, отправить сообщение; через 10 с снова исключить контакт | Задача отменяется при проверке перед отправкой | `844426`, входящее 08:59:36, `skipped_excluded` 09:00:36 UTC |
| M08 | Снять исключение, отправить сообщение; через 10 с ответить от владельца | Автоответ отменяется | `844427`, ответ владельца `844428`, `skipped_owner_replied` 09:02:35 UTC |
| M09 | В dry-run отправить вопрос об оплате | Денежная категория и денежный шаблон в превью | `844429`, `dry_run` 09:05:03 UTC; скриншот |
| M10 | Отправить сообщение; во время задержки нажать «Вимкнути» | Автоответ отменяется | `844431`, `skipped_kill_switch` 09:07:24 UTC |
| M11 | Возобновить; контакт наследует окно 22:00–08:00 Kyiv; отправить днём | Нет ответа вне расписания | `844432`, `skipped_schedule` 09:09:51 UTC |
| M12 | Открыть контакт, изменить общее расписание на другой час, сохранить, вернуться к контакту без переоткрытия | Унаследованное расписание сразу обновляется | Найден баг, исправлен; владелец подтвердил повторную проверку |
| M13 | При ночном общем окне установить контакту 00:00–23:59; отправить днём в dry-run | Персональное окно применяется; приходит превью | Пользователь подтвердил тест 10 |
| M14 | Включить live; отправить «Тест 11. Підкажіть, коли зможете відповісти?» | Один реальный ответ с подписью секретаря | Скриншот 16:42, подтверждение владельца |
| M15 | Нажать «Терміново» на реальном ответе | Предложение с ценой и кнопками подтверждения/отмены | Скриншот: 1000 UAH |
| M16 | Нажать «Скасувати», затем снова «Терміново»; повторить | Предложение удаляется; исходный ответ снова содержит кнопку | После двух исправлений и выдачи права удаления владелец подтвердил |
| M17 | Подтвердить платное предложение | Платное обращение учтено; уведомления клиенту и владельцу доставлены | Обращение 11, 1000 UAH; `paid-contact:11` и `paid-owner:11` завершены с одной попытки |
| M18 | В уведомлении владельца нажать «Відмовити» | Клиент получает отказ | Владелец подтвердил |
| M19 | Открыть месячный PDF за сентябрь из Telegram | PDF открывается; суммы соответствуют тестам | Скриншот: 2 платных обращения, 2000 UAH; владелец подтвердил два отдельных теста |

M07: первая попытка с уже включённым исключением не засчитывалась как проверка
отмены во время задержки; в таблице записан успешный повтор.

## Следующий сценарий: ручной ответ

### M20 — отправка из чата управления — НЕ ПРОЙДЕН

1. Оставить dry-run. У тестового контакта должно быть свежее входящее.
2. В чате с ботом нажать «🤖 Відправити від бота», выбрать тестовый контакт.
3. Проверить предупреждение о реальной немедленной отправке.
4. Ответить на запрос бота: «Тест 12. Ручна відповідь секретаря».
5. Ожидать одно сообщение у второго аккаунта и завершение формы у владельца.
6. Повторно ответить на уже обработанный запрос: второго сообщения быть не должно.

Сценарий был предложен владельцу; подтверждения выполнения пока нет.

## Оставшиеся проверки интерфейса и правил

| ID / статус | Шаги | Ожидаемый результат |
|---|---|---|
| M21 — не пройден | Открыть новое платное предложение после последнего обновления, прочитать текст, отменить | До подтверждения видны цена, отсутствие гарантии ответа и учёт в конце месяца |
| M22 — частично | Заново сформировать и открыть сентябрьский PDF | «Повідомлення» целиком на одной строке; цифры не изменились. Локальный рендер проверен, новый PDF в Telegram ещё не подтверждён |
| M23 — не пройден | Изменить поле контакта, перейти в другую вкладку и назад; отменить правки; отдельно сохранить | Черновик не пропадает молча; отмена восстанавливает сохранённое состояние; сохранение снимает признак изменений |
| M24 — не пройден | С несохранённым исключением/личным окном контакта сохранить другое общее окно и часовой пояс | Унаследованные данные актуальны; черновик контакта не сброшен. Персональное окно не заменено общим |
| M25 — не пройден | Задать паузу до ближайшего времени, сверить часовой пояс, дождаться окончания и отправить новое сообщение | До срока нет ответа, после срока действуют обычные правила; дата понятна в чате и Mini App |
| M26 — не пройден | Задать ночное окно и окна для будней/выходных; проверить превью на границах и переходе через полночь | День и интервал трактуются согласно расписанию, нет неожиданных ответов вне окна |
| M27 — не пройден | Сохранить изменённый обычный и денежный шаблон; отправить соответствующие тестовые сообщения | Превью использует сохранённый шаблон; подпись соответствует выбранному отправителю |
| M28 — не пройден | Установить лимит 1 на окно; отправить несколько сообщений подряд в новом тестовом окне | Только один допустимый автоответ/превью; остальные отражены как пропущенные по лимиту |
| M29 — не пройден | В истории найти тест по контакту, дате и результату; очистить фильтры | Статусы и время соответствуют фактическим действиям, фильтры не скрывают данные после сброса |
| M30 — не пройден | Открыть Mini App на телефоне: ширины около 320/390px, светлая/тёмная тема, крупный шрифт, экранная клавиатура | Поля читаемы, кнопки доступны, нет горизонтального переполнения и перекрытия сохранения |
| M31 — не пройден | Новый тестовый владелец проходит invite → подключение → расписание → dry-run без подсказок | Понятно, какие права нужны, что настроено и когда начнутся ответы; чужие данные недоступны |
| M32 — не пройден | После согласованного удаления права ответа у тестового подключения открыть статус и попробовать сценарий; затем вернуть право | Понятное сообщение о проблеме и восстановлении; нет ложного статуса успешной доставки |

## Саммари, хранение и отчёты

| ID / статус | Шаги | Ожидаемый результат |
|---|---|---|
| M33 — не пройден | В «Підсумки» выбрать тестовый канал, настроить ближайшее время; создать диалог с вопросом, дождаться выпуска | Саммари появляется в выбранном канале, отражает диалог без выдуманных договорённостей |
| M34 — не пройден | На карточке саммари нажать Resolve/«Позначити вирішеним», повторить действие | Вопрос закрывается один раз; повторное нажатие не увеличивает счётчик |
| M35 — не пройден | Нажать переход к чату, затем read-all/«Прочитати все» на выпуске | Открывается нужный чат; действие чтения корректно отражается, остальные карточки доступны |
| M36 — не пройден | На карточке нажать «Відповісти від бота», ответить на адресный запрос | Предупреждение о реальной отправке; сообщение приходит именно указанному контакту один раз |
| M37 — не пройден | Включить хранение на тестовом диалоге, проверить счётчики; выключить и проверить результат удаления | Поведение соответствует описанию настройки; после очистки тексты не остаются доступными через UI |
| M38 — не пройден | Без хранения создать денежное обращение; дождаться утреннего списка при соответствующей конфигурации | Приходит список на метаданных без сохранённых текстов; повторного выпуска того же списка нет |
| M39 — не пройден | Открыть PDF-ссылку в отдельной приватной сессии, затем попытаться открыть панель без Telegram-входа | Разрешён только отчёт в области PDF-токена; полноценная панель без отдельного входа недоступна |
| M40 — не пройден | Сформировать отчёт за пустой месяц и за месяц с несколькими контактами/длинными именами | Пустое состояние понятно; таблица читаема, строки не обрезаны, заголовки повторяются на страницах |

## Повторы, сбои и длительная приёмка

Проверки с отказами выполняются на изолированном тестовом стенде совместно
с разработчиком; их не следует вызывать нагрузкой на реальный Telegram.

| ID / статус | Шаги | Ожидаемый результат |
|---|---|---|
| M41 — не пройден в текущей версии | При ожидающей задаче в dry-run перезапустить app, дождаться срока | Задача не теряется, приходит одно превью; запись завершена. Исторический тест 28.08 не заменяет повтор |
| M42 — не пройден вживую | Повторить callback подтверждённого платного обращения на контролируемом стенде | Нет второго начисления и увеличения счётчика; автотест уже есть |
| M43 — не пройден | Дождаться более 24 часов без входящих от отдельного тестового контакта, сделать адресную ручную попытку | Понятный отказ по недоступному окну; нет ложного успеха и бесконечных повторов |
| M44 — не пройден | На тестовом стенде воспроизвести неизвестный исход отправки, открыть журнал и чат перед повтором | Не происходит автоматического дубля; владелец видит необходимость проверки |
| M45 — не пройден | На тестовом стенде задержать уведомление, восстановить доступ, проверить повтор; отдельно проверить кнопку повтора исчерпанных попыток | Уведомление восстанавливается; сумма не начисляется повторно; статус очереди понятен |
| M46 — не пройден | 7 дней ограниченного пилота; 3 дня сверять саммари с перепиской; записывать пропуски, ложные ответы, дубли и классификацию | Есть ежедневные результаты и решение о готовности, а не только успешный короткий smoke-тест |

## Исправленные находки и остатки

- Общее расписание не обновлялось в открытом контакте — исправлено, M12 пройден.
- Отмена платного предложения оставляла карточки и теряла путь обратно —
  исправлено, включая `message is not modified`; M16 пройден. Право
  `can_delete_sent_messages` выдано и подтверждено в БД.
- Условия оплаты были видны только после подтверждения — исправлен текст;
  нужна визуальная проверка M21.
- Заголовок PDF переносил одну букву — исправлено и проверено локальным
  рендером; осталось открыть новый отчёт в Telegram (M22).
- Третья кнопка оценки dry-run обрезалась («Позначити д…») — осталось проверить
  и улучшить расположение кнопок.
- Разница часовых поясов в паузе и статусе сбивала с толку — нужна проверка M25
  и при необходимости более явная подпись часового пояса.
- Денежный шаблон обещает ответ «уранці» даже при дневном персональном окне —
  требуется решение по формулировке и проверка M27.
- Отказ владельца не снимает начисление — текущее поведение, продуктовая
  приемлемость отдельно не подтверждена.

## Перед rollout и завершением тестов

- Вернуть согласованный режим и расписания; убрать только временные настройки
  тестового контакта. Не считать `/dryrun` отменой уже созданных начислений.
- Согласовать судьбу тестовых начислений (в проверенном PDF — 2000 UAH),
  прежде чем использовать отчёт для реального расчёта. Автоматически не удалять.
- Настроить стабильный HTTPS-адрес и управляемый запуск: текущий Quick Tunnel
  уже терял доступность и подходит только для стенда.
- Закрыть оставшиеся сценарии либо явно записать принятые ограничения.
  Не объявлять полный rollout завершённым по результатам одного дня.

## Как записывать следующий результат

Для каждого сценария: дата/время и пояс, версия или коммит, выполненные шаги,
режим и настройки, ожидаемый/фактический результат, ссылка на скриншот или
идентификатор тестового события, статус и найденный дефект. Отсутствие ответа
не считается успешным результатом, пока не проверена причина в журнале.
